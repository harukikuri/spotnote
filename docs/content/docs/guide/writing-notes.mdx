---
title: Writing Notes
description: Create, edit, copy, and manage notes
---

## Creating a note

1. Enter inspect mode (**hold `Alt`** or toggle Inspect in the launcher).
2. **Click** an element — a pin drops and the note panel opens.
3. Type an instruction and click **Add** (or press `Enter`).

The note is saved and its pin stays on the element. Adding a note copies the agent-ready prompt to your clipboard right away.

## Editing a note

Click an existing pin to reopen it. The panel shows a **copy** button (top-right), your note text prefilled, and **Cancel** / **Update** in the footer. The copy button turns into a green check when the prompt is on your clipboard.

## Deleting a note

Open the note and click the **trash** button. This removes the pin and its note.

## Storage

Notes and pins are stored in your browser's `localStorage` for the dev session. They persist across reloads but are local to your browser — nothing syncs across devices or teammates, and there's no server.

## The copied prompt

Copying a note produces a framework-neutral, agent-ready markdown prompt:

````markdown
## Request
make the padding bigger

## Target
- file: pages/Products.jsx
- line: 12, column: 4
- element: <button class="chip">
- text: "shoes"
- instance: 2 of 6 rendered from this line (by DOM order) — target the one matching the text above

## Current computed styles
  padding: 6px 12px
  font-size: 14px
  …
````

Paste it into Claude Code, Cursor, or Codex — it has the exact file/line, the element, and its computed styles, so the agent can make the edit without guessing. The `instance` line only appears when the element is one of several rendered from the same source line.
